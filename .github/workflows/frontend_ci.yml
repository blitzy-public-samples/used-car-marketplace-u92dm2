name: Frontend CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Use Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '14.x'

    - name: Install dependencies
      run: npm ci

    - name: Run linting checks
      run: npm run lint

    - name: Run unit tests
      run: npm test

    - name: Build frontend application
      run: npm run build

    - name: Deploy to staging
      if: github.ref == 'refs/heads/main'
      # HUMAN ASSISTANCE NEEDED
      # Please provide the specific deployment steps for your staging environment.
      # This could involve using a deployment service, copying files to a server, etc.
      # Example:
      # run: |
      #   npm install -g firebase-tools
      #   firebase deploy --only hosting:staging --token ${{ secrets.FIREBASE_TOKEN }}