// Loaded by Vitest via `test.setupFiles` in vite.config.ts. The jest-dom v5 entry
// point calls `expect.extend(...)` on the global `expect` (hence `test.globals: true`).
import '@testing-library/jest-dom';
